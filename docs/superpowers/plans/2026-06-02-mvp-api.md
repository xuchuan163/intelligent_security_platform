# MVP API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the documented MVP backend API surface for the CSCEC smart safety platform.

**Architecture:** Keep FastAPI endpoints thin, move reusable logic into services, and keep Pydantic request contracts in `backend/app/schemas`. Avoid expanding the second-phase Agent, memory, case, and report APIs in this task.

**Tech Stack:** FastAPI, Pydantic, SQLAlchemy, Pytest, MySQL/PyMySQL.

---

### Task 1: Add API Contract Tests

**Files:**
- Modify: `backend/tests/test_api_smoke.py`

- [ ] **Step 1: Add tests for missing MVP routes**

Add tests that call `/api/v1/rules/triggers`, `/api/v1/profile/recalculate`, and `/api/v1/assistant/project-risk-explanation`.

- [ ] **Step 2: Run tests and verify RED**

Run: `..\.venv\Scripts\python.exe -m pytest tests/test_api_smoke.py -v`

Expected: at least one failure or 404 because rules routing and profile recalculation are incomplete.

### Task 2: Wire Routes and Schemas

**Files:**
- Modify: `backend/app/api/v1/router.py`
- Modify: `backend/app/api/v1/endpoints/work_orders.py`
- Modify: `backend/app/api/v1/endpoints/assistant.py`
- Modify: `backend/app/schemas/work_orders.py`
- Modify: `backend/app/schemas/assistant.py`

- [ ] **Step 1: Mount `rules.router` under `/rules`**
- [ ] **Step 2: Remove duplicate `/triggers` endpoint from `work_orders.py`**
- [ ] **Step 3: Use `WorkOrderCreate`, `WorkOrderStatusUpdate`, `AssistantChatRequest`, and project explanation schemas in endpoints**
- [ ] **Step 4: Run API tests and continue to expected remaining failures**

### Task 3: Implement Profile Recalculation

**Files:**
- Modify: `backend/app/api/v1/endpoints/profiles.py`
- Modify: `backend/app/services/profiles/service.py`

- [ ] **Step 1: Add `recalculate_project_profiles(db)` service**
- [ ] **Step 2: Add `POST /profile/recalculate` endpoint**
- [ ] **Step 3: Run focused tests and verify GREEN**

### Task 4: Final Verification

**Files:**
- No production code changes expected.

- [ ] **Step 1: Run backend tests**

Run: `..\.venv\Scripts\python.exe -m pytest -v`

- [ ] **Step 2: Compile backend modules**

Run: `..\.venv\Scripts\python.exe -m compileall app`

- [ ] **Step 3: Smoke test running API if server is available**

Check `/api/v1/health`, `/api/v1/rules/triggers`, and `/api/v1/profile/recalculate`.

